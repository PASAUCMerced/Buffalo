// #include <vector>

// class Graph {
// public:
//     // We assume the graph is undirected, so we only need one adjacency list.
//     std::vector<std::vector<int>> adj_list;

//     // ... Other methods to add nodes and edges ...

//     // This function gets the in-edges of a node.
//     std::vector<int> in_edges(int node) {
//         return adj_list[node];
//     }
// };

// int main() {
//     Graph g;
//     // ... Add nodes and edges to the graph ...

//     // Get the in-edges of a node.
//     std::vector<int> in_edges = g.in_edges(0);

//     // Print the in-edges.
//     for (int edge : in_edges) {
//         std::cout << edge << std::endl;
//     }

//     return 0;
// }
